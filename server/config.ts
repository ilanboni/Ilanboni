/**
 * Configurazione dell'applicazione
 */
export const config = {
  // Numero di telefono dell'agente con prefisso internazionale
  // Il formato deve essere 39XXXXXXXXXX (senza il + iniziale)
  agentPhoneNumber: '393407992052'
};